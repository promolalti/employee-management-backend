<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="007-create-employee-table" author="promolati">

        <createTable tableName="employees">

            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"
                             primaryKeyName="pk_employees"
                             nullable="false"/>
            </column>

            <column name="employee_code" type="VARCHAR(50)">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_employees_employee_code"/>
            </column>

            <column name="first_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="middle_name" type="VARCHAR(100)"/>

            <column name="last_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="email" type="VARCHAR(150)">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_employees_email"/>
            </column>

            <column name="phone_number" type="VARCHAR(20)"/>

            <column name="gender" type="VARCHAR(20)"/>

            <column name="date_of_birth" type="DATE"/>

            <column name="joining_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="department_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="designation" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="reporting_manager_id" type="BIGINT"/>

            <column name="salary" type="DECIMAL(15,2)"/>

            <column name="employment_status"
                    type="VARCHAR(20)"
                    defaultValue="ACTIVE">
                <constraints nullable="false"/>
            </column>

            <column name="address" type="VARCHAR(500)"/>

            <column name="city" type="VARCHAR(100)"/>

            <column name="state" type="VARCHAR(100)"/>

            <column name="country" type="VARCHAR(100)"/>

            <column name="postal_code" type="VARCHAR(20)"/>

            <column name="created_by" type="VARCHAR(100)"/>

            <column name="created_at"
                    type="TIMESTAMP"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_by" type="VARCHAR(100)"/>

            <column name="updated_at"
                    type="TIMESTAMP"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- Department FK -->
        <addForeignKeyConstraint
                baseTableName="employees"
                baseColumnNames="department_id"
                referencedTableName="departments"
                referencedColumnNames="id"
                constraintName="fk_employees_department"/>

        <!-- Self Reference FK (Manager) -->
        <addForeignKeyConstraint
                baseTableName="employees"
                baseColumnNames="reporting_manager_id"
                referencedTableName="employees"
                referencedColumnNames="id"
                constraintName="fk_employees_reporting_manager"/>

        <!-- Indexes -->
        <createIndex tableName="employees"
                     indexName="idx_employees_department">
            <column name="department_id"/>
        </createIndex>

        <createIndex tableName="employees"
                     indexName="idx_employees_manager">
            <column name="reporting_manager_id"/>
        </createIndex>

        <createIndex tableName="employees"
                     indexName="idx_employees_email">
            <column name="email"/>
        </createIndex>

        <createIndex tableName="employees"
                     indexName="idx_employees_code">
            <column name="employee_code"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>