<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="009-create-password-reset-token-table" author="promolati">

        <createTable tableName="password_reset_tokens">

            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"
                             primaryKeyName="pk_password_reset_tokens"
                             nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="token" type="VARCHAR(500)">
                <constraints nullable="false"/>
            </column>

            <column name="expiry_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="used"
                    type="BOOLEAN"
                    defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>

            <column name="created_at"
                    type="TIMESTAMP"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- Foreign Key -->
        <addForeignKeyConstraint
                baseTableName="password_reset_tokens"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_password_reset_tokens_user"/>

        <!-- Unique Token -->
        <addUniqueConstraint
                tableName="password_reset_tokens"
                columnNames="token"
                constraintName="uk_password_reset_tokens_token"/>

        <!-- Indexes -->
        <createIndex tableName="password_reset_tokens"
                     indexName="idx_password_reset_tokens_user_id">
            <column name="user_id"/>
        </createIndex>

        <createIndex tableName="password_reset_tokens"
                     indexName="idx_password_reset_tokens_token">
            <column name="token"/>
        </createIndex>

        <createIndex tableName="password_reset_tokens"
                     indexName="idx_password_reset_tokens_expiry_date">
            <column name="expiry_date"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>